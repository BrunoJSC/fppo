"use client";

import { motion } from "framer-motion";

export default function About() {
  return (
    <motion.section
      id="projects"
      layoutId="projects"
      className="bg-[#FFD60A] min-h-screen"
    >
      <h1>About</h1>
    </motion.section>
  );
}
